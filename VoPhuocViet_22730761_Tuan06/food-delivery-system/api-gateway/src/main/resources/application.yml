server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      mvc:
        routes:
          - id: food-service
            uri: http://localhost:8081
            predicates:
              - Path=/api/v1/foods/**
            filters:
              - StripPrefix=0
